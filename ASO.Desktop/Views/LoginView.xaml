<Window x:Class="ASO.Desktop.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Iniciar sesión — Software ASO"
        Height="440" Width="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Segoe UI">

  <Grid Margin="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,20">
      <TextBlock Text="Software ASO" FontSize="22" FontWeight="Bold"
                 Foreground="{StaticResource PrimaryBrush}" />
      <TextBlock Text="Gestión de zafra" FontSize="12"
                 Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
    </StackPanel>

    <Border Grid.Row="1" Style="{StaticResource CardStyle}">
      <StackPanel>
        <TextBlock Text="Usuario" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
        <TextBox x:Name="UsuarioBox" Padding="8,6"
                  Text="{Binding NombreUsuario, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Contraseña" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                    Margin="0,14,0,4" />
        <PasswordBox x:Name="PasswordBox" Padding="8,6" KeyDown="OnPasswordKeyDown" />

        <TextBlock Text="{Binding MensajeError}" Foreground="{StaticResource DangerBrush}"
                    FontSize="12" TextWrapping="Wrap" Margin="0,10,0,0" />

        <Button Content="Iniciar sesión" Margin="0,18,0,0" Padding="8,10"
                Background="{StaticResource PrimaryBrush}" Foreground="White" BorderThickness="0"
                Cursor="Hand" Click="OnIniciarSesion" />

        <TextBlock Margin="0,16,0,0" FontSize="11" TextWrapping="Wrap"
                    Foreground="{StaticResource TextMutedBrush}"
                    Text="Usuarios de prueba: admin/admin123, operaciones/1234, taller/1234, finanzas/1234, rrhh/1234, consulta/1234" />
      </StackPanel>
    </Border>
  </Grid>
</Window>
