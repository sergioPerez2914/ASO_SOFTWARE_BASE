<Window x:Class="ASO.Desktop.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Iniciar sesión — Software ASO"
        Width="400" MinHeight="380" MaxHeight="720"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource BackgroundBrush}"
        FontFamily="Segoe UI">

  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <Grid Margin="32">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <StackPanel Grid.Row="0" Margin="0,0,0,20">
        <TextBlock Text="Software ASO" FontSize="22" FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryBrush}" />
        <TextBlock Text="Gestión de zafra" FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0" />
      </StackPanel>

      <Border Grid.Row="1" Style="{StaticResource CardStyle}">
        <StackPanel>
          <TextBlock Text="Usuario" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4" />
          <TextBox x:Name="UsuarioBox" Padding="8,6"
                    Text="{Binding NombreUsuario, UpdateSourceTrigger=PropertyChanged}" />

          <TextBlock Text="Contraseña" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"
                      Margin="0,14,0,4" />
          <PasswordBox x:Name="PasswordBox" Style="{StaticResource FormPasswordBoxStyle}"
                     KeyDown="OnPasswordKeyDown" />

          <TextBlock Text="{Binding MensajeError}" Foreground="{DynamicResource DangerBrush}"
                      FontSize="12" TextWrapping="Wrap" Margin="0,10,0,0" />

          <Button Content="Iniciar sesión" Margin="0,18,0,0" Padding="8,10"
                  Background="{DynamicResource PrimaryBrush}" Foreground="White" BorderThickness="0"
                  Cursor="Hand" Click="OnIniciarSesion" />
        </StackPanel>
      </Border>
    </Grid>
  </ScrollViewer>
</Window>
