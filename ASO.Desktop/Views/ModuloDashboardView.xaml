<UserControl x:Class="ASO.Desktop.Views.ModuloDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="640" d:DesignWidth="960">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- Encabezado del módulo -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,18">
      <TextBlock Text="{Binding Modulo.Icono}" FontFamily="{StaticResource IconFont}" FontSize="24"
                 Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center" Margin="0,0,12,0" />
      <StackPanel VerticalAlignment="Center">
        <TextBlock Text="{Binding Modulo.Nombre}" FontSize="20" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimaryBrush}" />
        <TextBlock Text="{Binding Modulo.Descripcion}" FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0" />
      </StackPanel>
    </StackPanel>

    <!-- Indicadores del módulo -->
    <ItemsControl Grid.Row="1" ItemsSource="{Binding Indicadores}" Margin="0,0,0,24">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <UniformGrid Rows="1" />
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource CardStyle}" Margin="0,0,12,0">
            <StackPanel>
              <TextBlock Text="{Binding Etiqueta}" FontSize="12" TextWrapping="Wrap"
                         Foreground="{DynamicResource TextSecondaryBrush}" />
              <TextBlock Text="{Binding Valor}" FontSize="26" FontWeight="Bold"
                         Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,4,0,0" />
              <TextBlock Text="{Binding Nota}" FontSize="11"
                         Foreground="{DynamicResource TextMutedBrush}" />
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>

    <TextBlock Grid.Row="2" Text="Submódulos" FontSize="14" FontWeight="SemiBold"
               Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10" />

    <!-- Lista de submódulos del módulo -->
    <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Submodulos}" VerticalAlignment="Top">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid Columns="3" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Button Style="{StaticResource CardButtonStyle}" Margin="0,0,12,12" Padding="18"
                    VerticalAlignment="Top"
                    AutomationProperties.Name="{Binding Nombre}"
                    Command="{Binding DataContext.AbrirSubmoduloCommand,
                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}">
              <StackPanel>
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding Icono}" FontFamily="{StaticResource IconFont}" FontSize="18"
                             Foreground="{DynamicResource AccentBlueBrush}" VerticalAlignment="Center" />
                  <TextBlock Text="{Binding Nombre}" FontSize="14" FontWeight="SemiBold"
                             Foreground="{DynamicResource TextPrimaryBrush}"
                             VerticalAlignment="Center" Margin="10,0,0,0" TextWrapping="Wrap" />
                </StackPanel>
                <TextBlock Text="{Binding Descripcion}" FontSize="12" TextWrapping="Wrap"
                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,10,0,0" />
              </StackPanel>
            </Button>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
