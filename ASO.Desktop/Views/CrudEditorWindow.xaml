<Window x:Class="ASO.Desktop.Views.CrudEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="{Binding Titulo}"
        Height="420" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Segoe UI">

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="{Binding Titulo}" FontSize="18" FontWeight="Bold"
               Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16" />

    <!-- El UserControl del formulario concreto se resuelve por DataTemplate implícito -->
    <ContentControl Grid.Row="1" Content="{Binding}" VerticalAlignment="Top" />

    <TextBlock Grid.Row="2" Text="{Binding ErrorValidacion}"
               Foreground="{StaticResource DangerBrush}" FontSize="12"
               TextWrapping="Wrap" Margin="0,8,0,0" />

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="Cancelar" Padding="14,8" Margin="0,0,8,0" Cursor="Hand"
              Background="Transparent" BorderBrush="{StaticResource BorderBrush}"
              Foreground="{StaticResource TextPrimaryBrush}"
              Command="{Binding CancelarCommand}" />
      <Button Content="Guardar" Padding="14,8" Cursor="Hand" BorderThickness="0"
              Background="{StaticResource PrimaryBrush}" Foreground="White"
              Command="{Binding GuardarCommand}" />
    </StackPanel>
  </Grid>
</Window>
