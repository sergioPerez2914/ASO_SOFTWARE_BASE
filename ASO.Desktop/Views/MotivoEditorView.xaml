<UserControl x:Class="ASO.Desktop.Views.MotivoEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="380">

  <StackPanel>
    <TextBlock Text="{Binding Descripcion}" FontSize="12" TextWrapping="Wrap"
               Foreground="{StaticResource TextSecondaryBrush}" />

    <TextBlock Text="{Binding EtiquetaMotivo}" Style="{StaticResource FormLabelStyle}" />
    <TextBox Style="{StaticResource FormTextBoxStyle}"
             AcceptsReturn="True" MinLines="3" TextWrapping="Wrap"
             VerticalContentAlignment="Top"
             Text="{Binding Motivo, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Text="Queda registrado junto con la decisión."
               FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,6,0,0" />
  </StackPanel>
</UserControl>
