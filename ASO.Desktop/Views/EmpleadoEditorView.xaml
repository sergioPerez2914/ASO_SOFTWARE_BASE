<UserControl x:Class="ASO.Desktop.Views.EmpleadoEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="280" d:DesignWidth="380">

  <StackPanel>
    <TextBlock Text="Nombre" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
    <TextBox Padding="8,6" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Text="Cédula" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,14,0,4" />
    <TextBox Padding="8,6" Text="{Binding Cedula, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Text="Cargo" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,14,0,4" />
    <TextBox Padding="8,6" Text="{Binding Cargo, UpdateSourceTrigger=PropertyChanged}" />

    <CheckBox Content="Activo" Margin="0,16,0,0" IsChecked="{Binding Activo}"
              Foreground="{StaticResource TextPrimaryBrush}" />
  </StackPanel>
</UserControl>
