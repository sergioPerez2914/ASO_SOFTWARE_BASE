<UserControl x:Class="ASO.Desktop.Views.InicioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="640" d:DesignWidth="960">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,20">
      <TextBlock Text="Inicio" FontSize="20" FontWeight="Bold"
                 Foreground="{StaticResource TextPrimaryBrush}" />
      <TextBlock Text="Elegí un módulo para ver su resumen y entrar a sus submódulos."
                 FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0" />
    </StackPanel>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Modulos}" VerticalAlignment="Top">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <UniformGrid Columns="3" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Button Style="{StaticResource CardButtonStyle}" Margin="0,0,12,12" Padding="18"
                    VerticalAlignment="Top"
                    AutomationProperties.Name="{Binding Nombre}"
                    Command="{Binding DataContext.AbrirModuloCommand,
                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}">
              <StackPanel>
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding Icono}" FontFamily="{StaticResource IconFont}" FontSize="20"
                             Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center" />
                  <TextBlock Text="{Binding Nombre}" FontSize="15" FontWeight="SemiBold"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             VerticalAlignment="Center" Margin="10,0,0,0" />
                </StackPanel>

                <TextBlock Text="{Binding Descripcion}" FontSize="12" TextWrapping="Wrap"
                           Foreground="{StaticResource TextSecondaryBrush}" Margin="0,10,0,0" />

                <ItemsControl ItemsSource="{Binding Submodulos}" Margin="0,12,0,0">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <TextBlock Text="{Binding Nombre}" FontSize="11" Margin="0,0,0,3"
                                 Foreground="{StaticResource TextMutedBrush}" />
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Button>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
