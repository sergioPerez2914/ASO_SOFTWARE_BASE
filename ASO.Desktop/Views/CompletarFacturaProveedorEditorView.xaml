<UserControl x:Class="ASO.Desktop.Views.CompletarFacturaProveedorEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:ASO.Desktop.Controls"
             mc:Ignorable="d"
             d:DesignWidth="380">

  <StackPanel>
    <TextBlock Text="{Binding Resumen}" FontSize="12" TextWrapping="Wrap"
               Foreground="{DynamicResource TextSecondaryBrush}" />

    <controls:FormField Etiqueta="Nº de documento">
      <TextBox Style="{StaticResource FormTextBoxStyle}"
               Text="{Binding NumeroDocumento, UpdateSourceTrigger=PropertyChanged}"
               ToolTip="El número que trae la factura del proveedor" />
    </controls:FormField>

    <controls:FormField Etiqueta="Vencimiento">
      <DatePicker Style="{StaticResource FormDatePickerStyle}"
                  SelectedDate="{Binding FechaVencimiento}" />
    </controls:FormField>

    <TextBlock Text="El proveedor, las líneas y el monto ya vienen de la orden de compra."
               FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,6,0,0" />
  </StackPanel>
</UserControl>
