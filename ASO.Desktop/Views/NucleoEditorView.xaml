<UserControl x:Class="ASO.Desktop.Views.NucleoEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="380">

  <StackPanel>
    <TextBlock Text="Código (C.O.D)" Style="{StaticResource FormLabelStyle}" />
    <TextBox Style="{StaticResource FormTextBoxStyle}"
             Text="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Text="Nombre" Style="{StaticResource FormLabelStyle}" />
    <TextBox Style="{StaticResource FormTextBoxStyle}"
             Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Text="El código es el que usa el sistema del CAM para atribuir el pago de corte, alza y transporte."
               FontSize="11" TextWrapping="Wrap"
               Foreground="{StaticResource TextMutedBrush}" Margin="0,6,0,0" />
  </StackPanel>
</UserControl>
