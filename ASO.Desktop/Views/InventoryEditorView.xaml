<UserControl x:Class="ASO.Desktop.Views.InventoryEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="440" d:DesignWidth="380">

  <StackPanel>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="12" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <StackPanel Grid.Column="0">
        <TextBlock Text="Código" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
        <TextBox Padding="8,6" Text="{Binding Codigo, UpdateSourceTrigger=PropertyChanged}"
                 IsEnabled="{Binding CodigoEditable}" />
      </StackPanel>

      <StackPanel Grid.Column="2">
        <TextBlock Text="Unidad" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
        <TextBox Padding="8,6" Text="{Binding Unidad, UpdateSourceTrigger=PropertyChanged}" />
      </StackPanel>
    </Grid>

    <TextBlock Text="Nombre" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,14,0,4" />
    <TextBox Padding="8,6" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Text="Categoría" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,14,0,4" />
    <TextBox Padding="8,6" Text="{Binding Categoria, UpdateSourceTrigger=PropertyChanged}" />

    <TextBlock Text="Ubicación" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,14,0,4" />
    <TextBox Padding="8,6" Text="{Binding Ubicacion, UpdateSourceTrigger=PropertyChanged}" />

    <Grid Margin="0,14,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="12" />
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="12" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>

      <StackPanel Grid.Column="0">
        <TextBlock Text="Stock actual" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
        <TextBox Padding="8,6" Text="{Binding StockActual, UpdateSourceTrigger=PropertyChanged}" />
      </StackPanel>

      <StackPanel Grid.Column="2">
        <TextBlock Text="Stock mínimo" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
        <TextBox Padding="8,6" Text="{Binding StockMinimo, UpdateSourceTrigger=PropertyChanged}" />
      </StackPanel>

      <StackPanel Grid.Column="4">
        <TextBlock Text="Costo unit." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
        <TextBox Padding="8,6" Text="{Binding CostoUnitario, UpdateSourceTrigger=PropertyChanged}" />
      </StackPanel>
    </Grid>
  </StackPanel>
</UserControl>
