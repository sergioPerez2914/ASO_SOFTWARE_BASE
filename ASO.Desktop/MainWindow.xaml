<Window x:Class="ASO.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ASO.Desktop.Controls"
        xmlns:views="clr-namespace:ASO.Desktop.Views"
        mc:Ignorable="d"
        Title="Software ASO — Gestión de Zafra"
        Height="800" Width="1280"
        MinHeight="640" MinWidth="1024"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        FontFamily="Segoe UI">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="220" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <!-- Barra de menú superior -->
    <Grid Grid.Row="0" Grid.ColumnSpan="2"
          Background="{StaticResource SurfaceBrush}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <Menu Grid.Column="0"
            Background="Transparent"
            FontSize="12" Padding="4,2">
        <MenuItem Header="_Archivo" />
        <MenuItem Header="_Ver" />
        <MenuItem Header="_Herramientas" />
        <MenuItem Header="A_yuda" />
      </Menu>

      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
        <TextBlock x:Name="UsuarioActualLabel" VerticalAlignment="Center" FontSize="12"
                    Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,10,0" />
        <Button Content="Cerrar sesión" FontSize="11" Padding="8,4" Cursor="Hand"
                Background="Transparent" BorderBrush="{StaticResource BorderBrush}"
                Foreground="{StaticResource TextPrimaryBrush}" Click="OnCerrarSesion" />
      </StackPanel>

      <Border Grid.ColumnSpan="2" VerticalAlignment="Bottom"
              BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" />
    </Grid>

    <!-- Sidebar -->
    <controls:Sidebar x:Name="MainSidebar" Grid.Row="1" Grid.Column="0"
                      BorderBrush="{StaticResource SidebarBorderBrush}" BorderThickness="0,0,1,0" />

    <!-- Contenido principal (cambia según la sección seleccionada) -->
    <ContentControl x:Name="ContentArea" Grid.Row="1" Grid.Column="1" Margin="20" />

  </Grid>
</Window>
